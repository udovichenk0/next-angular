import { Routes } from '@angular/router';
import { FindRoomComponent } from './pages/find-room/find-room.component';

export const routes: Routes = [
  { path: 'find-room', component: FindRoomComponent}
];